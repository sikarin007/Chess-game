[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pygame-chess"
version = "1.0.0"
description = "เกมหมากรุกสากล ปะทะ AI เขียนด้วย Pygame"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pygame>=2.5.0",
]
